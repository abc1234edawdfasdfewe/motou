# 墨投（MoTou）技术文档 — 跨平台开发参考

> 面向 iOS 端（及任何新发送端/接收端）开发的技术梳理。
> 读完本文即可在不读 Android 源码的情况下实现一个等价客户端。

---

## 0. 系统全景

墨投是**局域网内容推送系统**，不是镜像投屏。三端角色：

```
┌──────────────┐   WS/HTTP :8383    ┌────────────────────┐
│  发送端       │ ─────────────────▶ │  墨水屏设备端        │
│ (手机App/网页  │  内容 + 控制       │  (Ktor 服务器+渲染)   │
│  /iOS 待开发) │ ◀───────────────── │                     │
└──────────────┘  回执 + 反向请求     └────────────────────┘
```

- **设备端（接收端）**：墨水屏 Android 设备，跑一个 Ktor HTTP+WS 服务器（端口 8383），同时是渲染器。所有发送端都连它。**它不变**，iOS 开发只需实现发送端。
- **手机发送端（Android sender）**：原生 App，功能最全。
- **网页发送端**：设备端内置 HTTP 服务器直接吐出 `assets/web/` 页面，浏览器打开 `http://<设备IP>:8383` 即用，零安装。

iOS 端 = 实现「发送端」角色即可复用现有墨水屏设备端。

---

## 1. 技术栈总览

### 1.1 墨水屏设备端（`app/` 模块，包名 `com.motou.app`）

| 层 | 技术 |
|---|---|
| 语言 / 构建 | Kotlin 2.0.20，AGP 8.5.2，JDK 17，minSdk 26 / targetSdk 34 |
| 服务器 | Ktor 2.3.12（`ktor-server-cio` + `ktor-server-websockets`），内嵌 `embeddedServer`，host `0.0.0.0:8383` |
| 并发 | kotlinx-coroutines 1.8.1；`ContentBus` 用 `MutableSharedFlow` 在 Ktor 线程与 UI 主线程间解耦 |
| JSON | kotlinx-serialization-json 1.7.1 |
| 渲染 | Android `WebView`（排版/聊天页）+ `ImageView`（位图直显） |
| 二维码 | zxing core 3.5.3（生成待机页连接二维码） |
| 墨水屏控制 | Rockchip 私有 `eink` 系统服务，经编译期 stub + `IBinder.transact` 手写 Parcel 调 `setProperty("sys.ebook.mode", v)`（隐藏 API 反射被拦，故用 binder 直调） |
| 常驻 | `ServerService` 前台服务持有服务器进程 |

关键文件：
- `server/MoTouServer.kt` — HTTP 路由 + WS 会话
- `server/Protocol.kt` — 协议编解码（全部消息入口 `handleIncoming`）
- `server/ContentBus.kt` — 进程内消息总线
- `MainActivity.kt` — 单 Activity 多模式（待机/排版/位图/聊天）
- `util/Eink.kt` — 墨水屏刷新模式

### 1.2 安卓手机发送端（`sender/` 模块，包名 `com.motou.sender`）

| 层 | 技术 |
|---|---|
| 语言 / SDK | Kotlin，minSdk 26 / targetSdk 34 |
| 网络 | OkHttp 4.12.0（仅 WS 客户端 + 普通 HTTP）；LLM/OCR 用裸 `HttpURLConnection` |
| 相机/扫码 | CameraX 1.3.4 + zxing（扫设备待机页二维码即连） |
| 文件解析 | docx 手解 zip 读 `document.xml`；PDF 用系统 `PdfRenderer`；CBR/RAR 用 junrar 7.5.5（CBZ/ZIP 用 JDK `java.util.zip`） |
| 图像 | 自写 Floyd–Steinberg / 4×4 Bayer 有序抖动（`Dither.kt`）；EXIF 方向校正 androidx.exifinterface |
| 设备发现 | NSD（`_motou._tcp.` 服务广播/发现）+ 手填 IP + 扫码 |
| 后台 | `ChatLinkService`（dataSync 前台服务，锁屏续聊保活）+ 电池优化白名单（对抗 ColorOS Hans 冻结） |
| 反向控制 | 无障碍服务 `TouchService.dispatchGesture` 注入触摸 |
| 录屏 | MediaProjection（`CastLiveService`）→ VirtualDisplay → ImageReader 抓帧 |

关键文件：
- `SenderApp.kt` — Application，持有全局 `WsClient`、所有 WS 回调切主线程
- `WsClient.kt` — WS 客户端封装
- `ChatStore.kt` — AI 会话枢纽（历史/持久化/markdown→html/同步设备）
- `AiClient.kt` — OpenAI 兼容 LLM 客户端
- `OcrClient.kt` — PaddleOCR-VL 异步任务客户端
- `Discovery.kt` — NSD 发现

### 1.3 网页发送端（`app/src/main/assets/web/`，零构建原生 JS）

| 层 | 技术 |
|---|---|
| 结构 | 原生 HTML/CSS/JS，无框架无构建 |
| PDF | PDF.js（UMD 离线打包，`lib/pdf.min.js`） |
| 正文提取 | Readability.js（网址抓取后提正文） |
| docx | mammoth.browser.min.js |
| 抖动 | `dither-worker.js`（Web Worker 跑 Floyd–Steinberg） |
| 窗口捕获 | `getDisplayMedia`（需浏览器安全例外） |
| 存储 | localStorage（设置 / AI 会话 / 投送历史） |

---

## 2. 通信协议（iOS 端要实现的核心）

### 2.1 传输

- **WS 控制+内容通道**：`ws://<设备IP>:8383/channel`
  - 文本帧 = JSON 控制消息
  - 二进制帧 = 位图页数据（`page` JSON 帧**紧随其后**的二进制帧即该页 PNG/JPEG）
- **HTTP**：
  - `GET /` 与 `GET /{path}` → 网页发送端静态资源
  - `POST /fetch` → URL 抓取代理（body `{"url":"..."}`，返回 HTML，解 CORS）
  - `POST /proxy` → 通用 HTTP 代理（body `{url,method,headers,body|bodyBase64}`，返回 `{status,body}`，供网页端调 LLM/OCR）
  - `GET /debug/eink` → 墨水屏诊断

### 2.2 消息一览（type 字段，未知 type 静默忽略 = 向前兼容）

**连接建立，设备 → 发送端：**
```json
{"type":"hello","device":"rockchip rk3576_ebook",
 "screen":{"width":1404,"height":1788,"dpi":...,"density":...},
 "grayscale":16,"color":false,"protocol":2,"renderer":["html","bitmap"]}
```
→ 发送端据此适配分辨率与灰阶数。

**排版通道（文字/网页/docx/md，发送端 → 设备）：**
```json
{"type":"html","id":"c-xxx","title":"…","body":"<p>受控HTML</p>"}
{"type":"nav","id":"c-xxx","page":3}        // 双向翻页
{"type":"clear"}                             // 回待机页
```

**位图通道（图片/PDF/漫画/直播，发送端 → 设备）：**
```json
{"type":"content.begin","id":"c-xxx","kind":"bitmap","title":"…","pageCount":N,"live":false}
{"type":"page","id":"c-xxx","index":0,"format":"png"}   // 紧随二进制帧 = 该页图
{"type":"live.end"}                                     // 结束直播
```
`live:true` 进直播模式（设备切 A2 快刷）。

**设备 → 发送端回执：**
```json
{"type":"state","id":"…","page":0,"pages":12}   // 排版通道渲染/翻页回执
{"type":"rendered","id":"…","page":0}           // 位图页已上屏（触发预取窗口）
{"type":"nav","id":"…","page":3}                // 设备端翻页/缺页回请
```

**AI 会话：**
```json
// 发送端 → 设备：整段会话同步（设备进聊天页）
{"type":"chat","msgs":[{"role":"user","html":"<p>…</p>"},{"role":"assistant","html":"<p>…</p>"}]}
// 设备 → 发送端：墨水屏上追问
{"type":"chat.ask","text":"…"}
// 设备 → 发送端：阅读页选中文字「问 AI」
{"type":"text.ask","text":"选中的文字"}
```

**反向控制（设备 → 发送端，直播时）：**
```json
{"type":"touch","kind":"tap","x":0.5,"y":0.3}
{"type":"touch","kind":"swipe","x":..,"y":..,"x2":..,"y2":..}  // 归一化坐标 0..1
```

### 2.3 位图发送要点（iOS 必读）

1. 先 `content.begin`（带 `pageCount`）再逐页发。
2. 每页：发 `page` JSON 文本帧 → **紧接着**发该页二进制帧。
3. 设备按 `rendered` 回执推进预取窗口（当前页 -1…+2），也按 `nav` 缺页回请。
4. 图片处理：按 `hello.screen` 分辨率建画布 → 等比 fit 居中留白 → 灰度抖动到 `hello.grayscale` 级 → PNG（直播帧用 JPEG 省带宽）。
5. 直播：帧门控——上一帧未收到 `rendered` 回执不再叠加发送，超时丢帧；画面静止（缩略图采样对比）不发。

---

## 3. 外部服务集成（发送端职责）

### 3.1 LLM（OpenAI 兼容，DeepSeek/豆包/Kimi 通用）
- `POST {baseUrl}/chat/completions`（baseUrl 缺省补 `/v1`），`Authorization: Bearer <key>`
- body `{model, messages:[{role,content}]}`；`AiClient.kt` / 网页端 `sendChat` 是参考实现
- 用途：AI 对话、选中文字问 AI、设备追问（`chat.ask`）的应答

### 3.2 OCR（PaddleOCR-VL，异步任务）
1. 图片压缩：EXIF 校正 + 长边 ≤1920 + JPEG 0.85（相机原图 6-11MB 会超限）
2. `POST https://paddleocr.aistudio-app.com/api/v2/ocr/jobs`，multipart 字段名 `file`，头 `Authorization: bearer <TOKEN>`
3. 轮询 `GET /jobs/{jobId}` 至 `state=done`
4. 取 `resultUrl.jsonUrl` → JSONL，每行 `result.layoutParsingResults[i].markdown.text` 拼接 → 走排版通道投送

---

## 4. 平台适配坑（已踩过，iOS 注意对照）

| 坑 | 原因 / 解法 |
|---|---|
| 锁屏后设备追问无回复 | Android ColorOS Hans 冻结挂起进程但不断 TCP。解法：电池优化白名单（系统收到包唤醒 App）。**iOS 对照**：后台无法常驻 TCP，需用 Push 或提示用户保持前台 / 开后台模式 |
| Toast 崩 `Looper.prepare()` | WS 回调在 OkHttp 后台线程，UI 操作必须切主线程 |
| 设备重开进了旧聊天页 | 会话同步 Flow 误用 replay=1 重放旧数据；应 replay=0 只在线时实时收 |
| WebView 长按菜单崩 | 不能直接 `startActionMode`，要子类化 WebView 重写 `startActionMode(callback,type)` 包裹回调注入菜单项 |
| targetSdk 34 前台服务崩 | FGS 必须声明 `foregroundServiceType`（dataSync/mediaProjection）+ 对应权限 |
| 相机图 OCR 失败 | 原图超 10MB 读取上限 → 必须先压缩 |
| 中文路径构建 | Android SDK 经目录联接 `C:\motou-sdk` 规避中文路径 |

---

## 5. iOS 发送端最小可行清单

按优先级，参照「安卓 sender 功能对齐网页端」的取舍：

**P0（核心投送）**
- [ ] WS 客户端连 `/channel`，处理 hello（适配分辨率/灰阶）
- [ ] 文字 → 受控 HTML（`html` 消息）排版通道
- [ ] 图片 → 灰度抖动 → 位图通道（content.begin/page）
- [ ] PDF → 分页渲染位图（iOS 用 PDFKit/CoreGraphics）+ 预取窗口 + 缺页回请
- [ ] 历史记录 + 重投

**P1（差异化）**
- [ ] AI 对话（OpenAI 兼容）+ `chat` 同步设备 + 响应 `chat.ask` 追问
- [ ] OCR 投屏（PaddleOCR-VL）
- [ ] 分享扩展（Share Extension 接收文字/图片/文件）
- [ ] 网址投送（经设备 `/fetch` 代理 + 本地正文提取）

**P2（锦上添花）**
- [ ] 设备发现：Bonjour 浏览 `_motou._tcp.`（对应 Android NSD）
- [ ] 批量图片/漫画（CBZ 用 ZIPFoundation，CBR 需 Unrar）
- [ ] 书架/阅读进度
- [ ] 反向控制（iOS 受限于系统，基本不可行，可砍）

---

## 6. 仓库结构

```
墨投APP/
├── app/            # 墨水屏设备端（接收端 + 内置网页发送端）
│   └── src/main/
│       ├── java/com/motou/app/      # MainActivity / server / util
│       └── assets/
│           ├── renderer/            # reader.html(排版) chat.html(聊天)
│           └── web/                 # 网页发送端（原生JS零构建）
├── sender/         # 安卓手机发送端
│   └── src/main/java/com/motou/sender/
├── verify/         # 真机端到端测试脚本（Node，WS/CDP 驱动）
├── README.md       # 项目总览与里程碑
├── 技术方案.md      # 总体架构与设计决策
└── 墨投技术文档.md  # 本文档
```
