# 墨投（MoTou）技术方案

> 墨水屏内容投送 APP —— 不是镜像投屏，而是"电脑端排版、设备端即看"的局域网内容推送。

> 本文保留初始方案的设计背景。已落地能力和 Android / Web / iOS 三端可读文档矩阵以 [README](README.md#三端可读文档支持android--web--ios) 为准。

## 一、核心理念与总体架构

### 1.1 需求本质

- 电脑浏览器里**拖拽**文字 / 图片 / 文件 / 网址 → 墨水屏安卓设备**立刻显示排版好的内容**。
- 所有解析、处理尽量在电脑端完成；设备端零配置，不需要调字号、行距、格式。
- 局域网内直连，不依赖外网。

### 1.2 关键架构决策：安卓 APP 自带 Web 服务器，电脑端零安装

```
┌─────────────── 电脑（浏览器，零安装）───────────────┐
│  发送页（由安卓 APP 内置服务器提供）                  │
│  · 拖拽接收区（文件/文字/图片/URL）                   │
│  · 解析引擎：PDF.js / mammoth.js / Readability / marked │
│  · 渲染引擎：Canvas 按设备分辨率出图 + 灰度抖动        │
└──────────────┬──────────────────────────────────┘
               │  HTTP + WebSocket（局域网）
┌──────────────▼──────────────────────────────────┐
│  墨水屏安卓设备（墨投 APP）                          │
│  · 内置 HTTP/WS 服务器（Ktor，前台服务常驻）          │
│  · 能力上报：分辨率 / DPI / 灰阶 / 刷新模式            │
│  · 显示层：位图直显 或 模板化 WebView 排版             │
│  · 墨水屏刷新控制（Onyx SDK 适配 / 通用回退）          │
│  · 二维码配对 + mDNS 广播                            │
└─────────────────────────────────────────────────┘
```

**为什么这样设计：**

- 用户说"电脑端通过浏览器窗口拖拽" → 发送端就是一个网页。让**安卓 APP 内置 HTTP 服务器直接把这个网页提供给电脑浏览器**，电脑端什么都不用装：打开 APP → 扫二维码或输入 `http://192.168.x.x:端口` → 开始拖。
- 发送页由设备提供，意味着页面与设备**同源**，WebSocket 通信、URL 抓取代理都没有跨域问题。
- 排除的方案及原因：
  - **Miracast / Chromecast / DLNA**：面向视频流镜像，帧率优先、画质有损，与墨水屏"静态、高对比、低刷新"完全相反。
  - **scrcpy 类**：镜像电脑/手机屏幕，仍是"原格式"，不符合"内容重排版"的核心需求。
  - **网盘/传文件类（LocalSend 等）**：只传文件不排版，设备端还要自己打开，正是用户明确不要的。但 LocalSend 的 **mDNS 发现 + 内置服务器**模式值得借鉴。
  - **Snapdrop / PairDrop**：浏览器拖拽 + 局域网直传的交互范式与本项目发送端体验最接近，可作为前端交互参考。

### 1.3 连接握手：设备能力驱动一切排版

WebSocket 建立后设备先上报能力，电脑端所有渲染都以此为准：

```json
{
  "type": "hello",
  "device": "Onyx Boox Note Air 3",
  "screen": { "width": 1872, "height": 1404, "dpi": 227 },
  "grayscale": 16,
  "color": false,
  "refreshModes": ["full", "partial", "a2"],
  "renderer": ["bitmap", "html"]
}
```

电脑端据此决定：画布像素尺寸、字号的物理毫米→像素换算、是否做彩色→灰度抖动、分页高度。**这就是"投来即看"的技术保证**——排版永远针对这块屏幕定制。

## 二、内容处理管线（方案核心）

内容分两条通道，按"哪端渲染文字更好"划分，解析统一在电脑端：

### 2.1 通道 A：位图通道（版式类内容）

适用：**图片、PDF、扫描件**——这些内容本身就是"版式已定"的。

```
拖入 → 电脑端解析 → Canvas 按设备分辨率逐页渲染
     → 灰度化 + Floyd–Steinberg 抖动（Web Worker）
     → PNG（灰度，压缩率很好）→ WS 二进制帧逐页推送
     → 设备端解码位图直接上屏（整页全刷）
```

- PDF：**PDF.js** 在浏览器内渲染每页到 canvas，缩放到设备分辨率（保持页面比例，留白或智能裁边）。
- 图片：适配模式（fit / fill / 原始比例），彩色照片经抖动后在 16 灰阶屏上观感远好于直接量化。
- 大文档策略：只即时渲染当前页 ± 预取窗口（如前后 2 页），翻页指令回传电脑端按需渲染，避免一次性推几百页。

### 2.2 通道 B：排版通道（流式文字类内容）

适用：**选中文字、剪贴板、Markdown、网页正文、电子书和新旧 Office 可读内容**——这些内容需要"重新排版"。

```
拖入 → 电脑端解析清洗：
        · 纯文本 → 直接
        · Markdown → marked / markdown-it → HTML
        · docx → mammoth.js → 干净 HTML
        · EPUB / MOBI / AZW / AZW3 → 按阅读顺序提取章节 → 干净 HTML
        · DOC / PPT / PPTX / XLS / XLSX → 提取段落、幻灯片文字或单元格显示值 → 干净 HTML
        · URL → 经设备代理抓取 → Mozilla Readability 提取正文 → 干净 HTML
     → 统一为「受控 HTML 子集 + 排版参数」（白名单标签，剥掉所有原始样式）
     → WS 推送到设备
     → 设备端隐藏 WebView 用内置排版模板渲染（CSS 分页），直接上屏
```

**为什么文字类在设备端做最终渲染而不是电脑端出位图：**

1. 浏览器无法可靠地把任意 DOM 截图为位图（html2canvas 对复杂样式支持不全），文字走位图通道会有排版翻车风险。
2. 墨水屏上**原生渲染的文字远比位图文字锐利**（位图缩放会糊边，灰阶抗锯齿被抖动破坏）。
3. 设备端渲染时仍然**零配置**：字体、字号（按 DPI 换算物理大小，例如正文 3.5mm 高）、行距、边距全部由 APP 内置模板 + 电脑端下发的排版参数决定，用户不需要碰任何设置；但保留了"设备上一键调大字号"的能力（重排即可，不用重传）。

这符合"解析、处理**尽量**放在电脑端"：重活（格式解析、正文提取、清洗）全在电脑端，设备端只做最后一步光栅化，且模板完全受控。

### 2.3 URL 的跨域问题与解决

浏览器页面直接 fetch 任意网址会被 CORS 拦截。方案：设备端服务器提供 `POST /fetch` 代理接口（设备去抓取目标 URL 的 HTML 返回给发送页），发送页再跑 Readability 提取正文。发送页与设备同源，天然无跨域问题。（备选：复杂反爬网页可回退为"设备 WebView 直接打开 URL + 阅读模式"。）

## 三、通信协议

传输层：HTTP（静态页 + 代理）+ WebSocket（控制 + 内容），全部局域网内。

| 端点 | 用途 |
|---|---|
| `GET /` | 发送页（打包在 APK assets 里，离线可用） |
| `WS /channel` | 双向通道：JSON 控制消息 + 二进制内容帧 |
| `POST /fetch` | URL 抓取代理（解 CORS） |

消息流（JSON 控制帧 + 紧随的二进制数据帧）：

```
PC → 设备:
  content.begin  { id, kind: "bitmap"|"html", title, pageCount }
  page           { id, index, format: "png" }   + 二进制帧
  html           { id, body, style: {fontScale, margin, ...} }
  nav            { id, page }                    // 电脑端遥控翻页
  clear          {}

设备 → PC:
  hello          { ...能力描述，见 1.3 }
  rendered       { id, page }                    // 已上屏确认
  nav            { id, page }                    // 设备端翻页（同步回发送页）
  error          { code, message }
```

翻页双向同步：电脑端发送页显示缩略图/页码条可遥控翻页；设备端实体键/触摸翻页也回传，两端状态一致。

## 四、安卓端技术栈

| 模块 | 选型 | 说明 |
|---|---|---|
| 语言/UI | Kotlin + Jetpack Compose | 壳 UI 很薄：二维码页、连接状态、设置 |
| 内置服务器 | **Ktor (CIO)** | HTTP + WebSocket 一体，Kotlin 原生；备选 NanoHTTPD（更老设备兼容） |
| 常驻 | Foreground Service + WakeLock | 保证熄屏不断连；墨水屏设备通常常亮 |
| 发现配对 | 二维码（主）+ NSD/mDNS 广播（辅） | 二维码内容即 `http://ip:port`，最稳 |
| 位图显示 | 全屏 Activity + 自绘 View | 直接 draw 灰度 Bitmap，控制刷新时机 |
| 文字渲染 | 隐藏 WebView + 内置排版模板 | CSS 分页（column 或 JS 分页）；字体随 APK 打包（思源宋体/黑体）保证一致性 |
| 墨水屏刷新 | 适配层接口 `EinkRefresher` | Onyx 设备接 **Onyx SDK（EpdController，见 OnyxAndroidDemo 开源仓库）**：翻页用全刷（GC16）去残影，指示器用局刷；非 Onyx 设备回退为普通绘制，交给固件处理 |
| minSdk | 26（Android 8.0） | 主流墨水屏设备（Boox / Bigme / 汉王 / 掌阅）多为 Android 8–11 |

## 五、电脑端（Web 发送页）技术栈

打包进 APK assets，由设备服务器直接提供，**离线可用、无需部署**。

| 模块 | 选型 |
|---|---|
| 框架/构建 | Svelte（或 Vue3）+ Vite + TypeScript，产物体积小 |
| 拖拽 | HTML5 Drag & Drop API + 剪贴板粘贴（Ctrl+V 同样触发投送） |
| PDF 解析渲染 | **PDF.js**（Mozilla） |
| docx 转换 | **mammoth.js**（docx → 干净 HTML） |
| 网页正文提取 | **@mozilla/readability** |
| Markdown | marked / markdown-it |
| 电子书 / Office 语义导入 | `document-import.js` + fflate / SheetJS / ppt-to-text / lingo-reader（三端各有对应原生实现） |
| 灰度抖动 | 自写 Floyd–Steinberg，跑在 Web Worker，不卡 UI |
| 发送页 UI | 拖拽区 + 设备信息卡片 + 当前内容缩略图 + 翻页遥控 + 历史记录（localStorage） |

## 六、可借鉴的开源项目

| 项目 | 借鉴什么 |
|---|---|
| **PairDrop / Snapdrop** | 浏览器拖拽 + 局域网直传的交互与 WS 信令设计 |
| **LocalSend** | mDNS 设备发现、局域网 REST 协议、多平台经验 |
| **OnyxAndroidDemo**（onyx-intl） | Onyx 墨水屏刷新控制 SDK 的官方示例 |
| **PDF.js / mammoth.js / Readability / marked** | 全部解析能力的现成轮子，浏览器内运行 |
| **KOReader** | 墨水屏排版参数（字号/行距/边距的物理尺寸换算）经验参考 |

## 七、开发路线图

**M1 — 打通链路（骨架）**
APP 内置 Ktor 服务器 + 二维码；发送页拖入/粘贴纯文本 → 排版通道 → 设备 WebView 模板显示。验证"拖一段文字，1 秒内上屏排好版"。

**M2 — 图片与 PDF（位图通道）**
图片拖入 → 灰度抖动 → 上屏；PDF.js 分页渲染 + 按需预取 + 双向翻页同步。

**M3 — 网页与 docx**
`/fetch` 代理 + Readability 正文提取；mammoth.js docx 支持；发送页历史记录与重投。

**M4 — 墨水屏精修与体验**
Onyx SDK 刷新适配（全刷/局刷策略、去残影）；断线自动重连；设备端一键调字号（本地重排）；内置中文字体；多设备同时投送（可选）。

## 八、风险与预案

| 风险 | 预案 |
|---|---|
| 任意 URL 抓取被反爬/需登录 | 回退方案：提示用户在电脑上全选正文文字直接拖拽 |
| 老设备 WebView 内核旧，CSS 行为不一致 | 排版模板只用保守 CSS（无 grid/现代特性），随 APK 打包字体 |
| 超大 PDF 内存压力 | 页级按需渲染 + 预取窗口，永不整本驻留 |
| 局域网隔离（AP 隔离的路由器） | 连接失败时提示排查；提供 USB 网络共享兜底说明 |
| 彩色墨水屏（Kaleido 等）将来支持 | 能力握手已含 `color` 字段，抖动算法换成彩色抖动即可 |
| 局域网内他人乱投 | 可选配对 PIN（首次连接设备弹码确认） |
