<?xml version="1.0" encoding="utf-8"?>
<!-- 墨水屏规范：纯白底纯黑字、无阴影无灰色填充、无转场（visibility 切换） -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <LinearLayout
        android:id="@+id/standby"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="@string/app_name"
            android:textColor="#000000"
            android:textSize="48sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/qr"
            android:layout_width="280dp"
            android:layout_height="280dp"
            android:layout_marginTop="40dp"
            android:contentDescription="@string/qr_desc" />

        <TextView
            android:id="@+id/urlText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:fontFamily="monospace"
            android:textColor="#000000"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/waiting"
            android:textColor="#000000"
            android:textSize="16sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:gravity="center"
            android:text="@string/standby_hint"
            android:textColor="#000000"
            android:textSize="14sp" />

        <!-- M4 已保存内容列表（空时隐藏） -->
        <TextView
            android:id="@+id/savedTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="已保存内容（点按打开，长按删除）"
            android:textColor="#000000"
            android:textSize="16sp"
            android:visibility="gone" />

        <ScrollView
            android:id="@+id/savedScroll"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:layout_marginTop="8dp"
            android:visibility="gone">

            <LinearLayout
                android:id="@+id/savedList"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </ScrollView>
    </LinearLayout>

    <com.motou.app.ReaderWebView
        android:id="@+id/reader"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- AI 对话：聊天模式 WebView（chat.html） -->
    <WebView
        android:id="@+id/chat"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- M2 位图通道：整页灰度位图直显 -->
    <ImageView
        android:id="@+id/bitmap"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#FFFFFF"
        android:contentDescription="@string/bitmap_desc"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <!-- M4 阅读格式面板：文字模式中央点按唤起 -->
    <include layout="@layout/panel_format" />

    <!-- M4 位图操作条：位图模式中央点按唤起 -->
    <include layout="@layout/panel_bitmap" />

</FrameLayout>
