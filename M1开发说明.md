# 墨投 M1 开发说明书（交接给实现方）

> 本文档自成一体，实现方无需阅读其他材料即可动手。总体架构见同目录《技术方案.md》，本文只覆盖 M1。

## 0. M1 目标与验收标准

**目标**：打通最小闭环——墨水屏安卓设备打开 APP 显示二维码 → 电脑浏览器扫码/输 URL 打开发送页 → 拖拽或粘贴一段文字 → 设备端 1 秒内显示排版好的正文，可整屏翻页，电脑端可遥控翻页且页码双向同步。

**验收清单**（全部通过才算 M1 完成）：
1. APP 启动即显示待机页：二维码 + `http://IP:8383` 地址 + 连接状态文字。
2. 电脑浏览器（Chrome/Edge）打开该地址，发送页自动连上 WebSocket，显示设备型号与分辨率。
3. 在发送页拖入一段选中文字 / Ctrl+V 粘贴 / 文本框输入后点发送，设备端立即切换到阅读模式并显示排版内容。
4. 拖入 .txt / .md 文件同样生效（.md 按纯文本处理，M3 再解析）。
5. 设备端点按屏幕右 1/3 下一页、左 1/3 上一页，整屏无动画瞬时翻页；中间 1/3 点按切换页脚（页码指示）显隐。
6. 电脑端发送页显示"第 x / y 页"，点上一页/下一页按钮设备同步翻页；设备端翻页发送页页码同步更新。
7. 设备端按返回键回到待机页；断开 WiFi 重连后发送页自动重连（2 秒轮询）。
8. 全程无任何过渡动画、无涟漪效果；界面纯白底纯黑字。

**M1 明确不做**：图片/PDF（M2）、网址正文提取与 docx（M3）、Onyx 刷新 SDK / mDNS / 配对 PIN / 多设备（M4）。发送页拖入图片或网址时弹 toast 提示"后续版本支持"，不发送。

---

## 1. 技术选型与版本（务必照此，理由附后）

| 项 | 选型 | 理由 |
|---|---|---|
| 语言 | Kotlin 2.0.x | — |
| UI | **经典 View + XML，不用 Compose、不用 AppCompat/Material 库** | 墨水屏要零动画零涟漪，经典 View 行为可预测、依赖极少、包体小；按钮用 selector 做"按压黑白反色"即时反馈 |
| 内置服务器 | **Ktor 2.3.12**（ktor-server-cio + ktor-server-websockets） | HTTP+WS 一体，Kotlin 原生协程 |
| 序列化 | kotlinx-serialization-json 1.7.x（仅用 JsonObject 手动构建/解析，**不用多态序列化**） | 协议简单，手动解析最稳 |
| 二维码 | com.google.zxing:core 3.5.3 | 纯 Java 生成 Bitmap，无相机依赖 |
| 协程 | kotlinx-coroutines-android 1.8.x | — |
| SDK | minSdk 26 / targetSdk 34 / compileSdk 34；AGP 8.5.x；Gradle 8.9；JDK 17 | 墨水屏设备多为 Android 8–11 |
| 发送页 | **原生 HTML/CSS/JS，零框架零构建步骤**，直接放 assets | M1 不引入 Node 工具链，M2 需要 PDF.js 时再上 Vite |
| 设备端渲染 | 隐藏 WebView 加载 `file:///android_asset/renderer/reader.html` | 排版模板受控 |

其他 Gradle 要点：`android.useAndroidX=true`（AGP 要求，虽然不用 androidx 依赖）；packaging 排除 `META-INF/{AL2.0,LGPL2.1,INDEX.LIST,io.netty.versions.properties}`；Manifest 加 `android:usesCleartextTraffic="true"`（局域网 HTTP）。

---

## 2. 工程结构（完整文件清单）

```
墨投APP/
├── settings.gradle.kts            rootProject.name = "MoTou"; include(":app")
├── build.gradle.kts               三个插件 apply false：com.android.application 8.5.2、
│                                  org.jetbrains.kotlin.android 2.0.20、
│                                  org.jetbrains.kotlin.plugin.serialization 2.0.20
├── gradle.properties / gradle wrapper / .gitignore
└── app/
    ├── build.gradle.kts           applicationId com.motou.app
    └── src/main/
        ├── AndroidManifest.xml
        ├── java/com/motou/app/
        │   ├── MainActivity.kt            # 单 Activity，待机/阅读双模式（见 §5）
        │   ├── server/MoTouServer.kt      # Ktor：静态资源 + WS /channel（见 §4）
        │   ├── server/ServerService.kt    # 前台服务，持有 MoTouServer
        │   ├── server/Protocol.kt         # 协议 JSON 构建/解析辅助
        │   ├── server/ContentBus.kt       # 服务器↔界面 的进程内消息总线（见 §3）
        │   └── util/Net.kt  util/QrCode.kt
        ├── assets/
        │   ├── renderer/reader.html       # 设备端排版模板，CSS+JS 内联单文件（见 §6）
        │   └── web/index.html + app.js + style.css   # 电脑端发送页（见 §7）
        └── res/
            ├── layout/activity_main.xml   # FrameLayout：standby(LinearLayout) + WebView
            ├── values/strings.xml, themes.xml
            └── drawable/ic_launcher.xml   # 简单矢量图标（白底黑框方块即可）
```

---

## 3. 核心解耦：ContentBus（进程内总线）

服务器（Ktor 协程线程）与界面（主线程）之间**只通过 Kotlin Flow 通信**，互不持有引用：

```kotlin
object ContentBus {
    data class RenderContent(val id: String, val title: String, val body: String) // body 为受控 HTML
    val render      = MutableSharedFlow<RenderContent>(replay = 1, extraBufferCapacity = 4) // PC→设备：投送内容。replay=1 使 Activity 重建后能恢复最后内容
    val nav         = MutableSharedFlow<Int>(extraBufferCapacity = 8)     // PC→设备：翻到第 n 页(0-based)
    val toPc        = MutableSharedFlow<String>(extraBufferCapacity = 16) // 设备→PC：JSON 字符串，由 WS 会话统一转发
    val connections = MutableStateFlow(0)                                 // 当前连接的电脑数，待机页显示用
}
```

WebView 桥接回调在 JS 线程触发，一律用 `tryEmit`（buffer 已留够）。

---

## 4. 服务器端（MoTouServer + ServerService）

### 4.1 路由

| 路由 | 行为 |
|---|---|
| `GET /` | 返回 assets `web/index.html` |
| `GET /{path...}` | 按路径返回 assets `web/<path>`，Content-Type 按扩展名映射（html/js/css/png/svg/json），找不到返 404 |
| `WS /channel` | 见 4.2 |

端口固定 **8383**，host `0.0.0.0`，`embeddedServer(CIO, ...)`，`start(wait = false)`。注意：Ktor 静态文件插件读不了 Android assets，必须自写 handler（`context.assets.open(path).readBytes()` + `respondBytes`）。

### 4.2 WS 会话流程

1. 连接建立 → `connections.value++` → **服务器立即主动发 `hello`**（见 §8 协议）。屏幕参数用 `context.resources.displayMetrics` 的 widthPixels/heightPixels/xdpi/density（M1 精度足够）。
2. `launch { ContentBus.toPc.collect { send(it) } }` 把设备侧消息转发给此会话。
3. 循环消费 incoming 文本帧，`Json.parseToJsonElement` 后按 `type` 字段分发：`"html"` → emit 到 `render`；`"nav"` → emit 到 `nav`。未知 type 忽略。
4. 断开（finally）→ 转发 job cancel，`connections.value--`。多个电脑同时连接：都允许，toPc 广播给所有会话（M1 不做互斥）。
5. `install(WebSockets) { pingPeriod = Duration.ofSeconds(20) }` 保活。

### 4.3 ServerService（前台服务）

- `onCreate`：建通知渠道 → `startForeground`（通知文案：`墨投正在接收 · http://<ip>:8383`）→ 启动 MoTouServer。`onDestroy` 停服务器。
- targetSdk 34 前台服务类型：Manifest 中 `android:foregroundServiceType="specialUse"` + 子元素 `<property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="lan-content-cast"/>`；权限 `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_SPECIAL_USE` + `INTERNET` + `POST_NOTIFICATIONS`。API≥29 用三参 `startForeground(id, notif, FOREGROUND_SERVICE_TYPE_SPECIAL_USE)`，低版本用两参。
- MainActivity `onCreate` 里 `startForegroundService()` 启动它。

---

## 5. 设备端界面（MainActivity）——墨水屏设计规范

### 5.1 关键架构决定：单 Activity 双模式

**不要做两个 Activity。** Android 10+ 禁止后台（含前台服务）启动 Activity，若内容到达时用服务拉起新 Activity 会静默失败。方案：MainActivity 内一个 FrameLayout 装两个子视图——`standby`（待机页）和 `WebView`（阅读模式），内容到达时切 visibility。这同时天然满足"无转场动画"。

### 5.2 墨水屏 UI 全局规范（本项目所有界面必须遵守）

1. **纯白底（#FFFFFF）纯黑字（#000000）**，不用任何灰色大面积填充、不用阴影/elevation，分隔一律用 1–2dp 黑色实线边框。
2. **零动画**：theme 里 `android:windowAnimationStyle=@null`；不用任何 ObjectAnimator/transition；WebView 内所有 CSS `transition/animation` 禁用。
3. **点按反馈用瞬时黑白反色**，不用涟漪：按钮背景 selector——默认白底黑字黑边框 2dp，pressed 状态黑底白字。
4. **整屏翻页，禁止滚动**：任何超屏内容都分页显示，不出现滚动条。
5. 大点按目标（按钮最小 56dp 高），字体偏大。
6. 阅读模式 `FLAG_KEEP_SCREEN_ON`。
7. 状态栏白底黑字（`windowLightStatusBar=true`）。
8. theme parent 用 `android:Theme.Material.Light.NoActionBar`。

### 5.3 待机页（standby）内容

垂直居中排列：APP 名"墨投"（大号衬线体）→ 二维码 ImageView（约 280dp，zxing 生成，MARGIN hint=1）→ URL 明文（等宽字体，方便手输）→ 状态行（collect `connections`："等待电脑连接…" / "已连接 n 台电脑"）→ 一行操作提示（"电脑浏览器打开上方地址，拖入文字即投"）。IP 取自 `NetworkInterface.getNetworkInterfaces()` 里第一个非回环、site-local 的 IPv4；取不到显示"未连接 WiFi"。

### 5.4 阅读模式与 WebView 桥接

- WebView 配置：`javaScriptEnabled=true`、`allowFileAccess=true`、白色背景、关闭滚动条与 overScroll、`loadUrl("file:///android_asset/renderer/reader.html")`（onCreate 就预加载，切换时仅改 visibility）。
- **就绪门控**：`WebViewClient.onPageFinished` 前到达的内容先存 `pendingPayload`，就绪后补渲染。
- Native → JS：`evaluateJavascript("render(" + JSONObject.quote(payloadJson) + ")", null)` 与 `evaluateJavascript("goTo(n)", null)`。payload 是 JSON 字符串再经 `JSONObject.quote` 转义（org.json 自带），JS 侧 `render(str)` 内部 `JSON.parse`。**必须在主线程调用**（flow collect 已在 MainScope）。
- JS → Native：`addJavascriptInterface(Bridge(), "MoTou")`；`Bridge` 仅一个方法 `@JavascriptInterface fun onState(id: String, page: Int, pages: Int)` → `toPc.tryEmit(state JSON)`。渲染完成和用户翻页都走它（兼作 rendered 回执与页码同步）。
- **payload 字段与物理字号换算（关键坑）**：WebView 视口 `initial-scale=1` 下 1 CSS px = 1 dp。要让正文字高恒为物理 3.6mm、页边距 7mm，须换算：`cssPx = xdpi / 25.4 * mm / density`（xdpi、density 来自 displayMetrics）。payload = `{id, title, body, fontPx, padPx}`，fontPx/padPx 由 Kotlin 算好传入。
- 返回键：阅读模式 → 切回待机页（WebView gone，不清内容）；待机页 → 默认行为退出。

---

## 6. 设备端排版模板 reader.html（单文件，CSS+JS 内联）

### 6.1 排版样式

- `<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">`
- 白底黑字；正文衬线体 `font-family: serif`（系统字体，M4 再打包思源字体）；`line-height:1.7; text-align:justify`；中文段落 `p { text-indent:2em; margin:0 0 .8em }`；标题渲染为 `<h1>`（1.4em，加粗）。字号 = `var(--font)`、边距 = `var(--pad)`，由 render() 用 payload 的 fontPx/padPx 写入 CSS 变量。
- `-webkit-tap-highlight-color: transparent`；全局无 transition。

### 6.2 CSS 多列分页原理（核心算法）

结构：`#viewport`（fixed 全屏 overflow:hidden）包 `#content`。

```css
#content {
  box-sizing: border-box; height: 100vh;
  padding: var(--pad);
  column-fill: auto;
  column-width: calc(100vw - var(--pad) * 2);
  column-gap: calc(var(--pad) * 2);
}
```

如此每"列"即一页，且 **列宽+列距 恰好 = 100vw**，翻页只需 `#content { transform: translateX(-page * window.innerWidth px) }`（JS 设置内联 style，无 transition → 瞬时整屏翻页，e-ink 友好）。

页数计算（渲染后 `requestAnimationFrame` 里执行）：
```js
const step = window.innerWidth;
pages = Math.max(1, Math.ceil((content.scrollWidth - pad * 2 + gap) / step));
```

### 6.3 交互

- `#viewport` 上一个 click 监听：`x < 35%宽` → 上一页；`x > 65%` → 下一页；中间 → 切换页脚显隐。
- 页脚：fixed 底部白底、顶部 1px 黑线，文案 `第 {page+1} / {pages} 页 · 按返回键回接收页`，默认隐藏。
- 每次 `goTo(n)`（含 render 后的 goTo(0)）执行 `MoTou.onState(contentId, page, pages)` 上报。
- 暴露的全局函数：`render(payloadStr)`、`goTo(n)`。title 在 JS 内插入 h1 前须 HTML 转义（body 不转义——发送端已保证是受控 HTML）。

---

## 7. 电脑端发送页（assets/web，原生 JS）

### 7.1 界面（正常 LCD 屏，可用现代审美，风格黑白简洁呼应品牌）

- 顶栏：LOGO"墨投"+ 连接状态（●已连接 <设备名> W×H / ○连接中…）。
- 主体：大拖拽区（虚线边框，dragover 时高亮反色），提示"拖入文字、txt/md 文件，或直接 Ctrl+V"。
- 拖拽区下：textarea + "投送"按钮（Ctrl+Enter 快捷发送）。
- 底部控制条（有内容后出现）：「上一页」「第 x / y 页」「下一页」。
- 右下角 toast 容器（3 秒自动消失，用于提示不支持类型等）。

### 7.2 逻辑

- **WS 连接**：`ws://${location.host}/channel`，onclose 后 2 秒重连（无限次）；收 `hello` 存设备信息并更新顶栏；收 `state` 更新页码条。
- **拖拽**：window 级 dragover/drop 全部 preventDefault（防浏览器打开文件）。drop 分发：
  - `dataTransfer.files` 非空：`.txt/.md` 用 FileReader 读文本→投送；图片类型→toast "图片投送将在 M2 支持"；其他→toast "暂不支持该格式"。
  - 否则取 `getData('text/plain')`；若整体是 URL（`/^https?:\/\/\S+$/`）→ toast "网址正文提取将在 M3 支持"，不发送；否则投送。
- **粘贴**：document paste 事件，取 text/plain（焦点在 textarea 时不拦截，走默认行为）。
- **文本 → 受控 HTML**（解析在电脑端的 M1 体现）：
  1. HTML 转义（& < >）。
  2. 标题启发：文本有多行且首行 ≤ 30 字 → 首行作 title，其余作正文；否则 title 为空。
  3. 按空行（`\n{2,}`）分段包 `<p>`，段内单个 `\n` 转 `<br>`。
- **投送消息**：`{type:"html", id: <随机id>, title, body}`；随后的翻页按钮发 `{type:"nav", page:n}`（n 由本地页码状态 ±1，0-based，边界钳制）。

---

## 8. 通信协议（M1 全量定义）

WS 文本帧，一帧一条 JSON。所有消息必有 `type` 字段。

**设备 → PC：**

```jsonc
// 连接建立后服务器立即发送
{ "type": "hello",
  "device": "<Build.BRAND Build.MODEL>",
  "screen": { "width": 1872, "height": 1404, "dpi": 227.0, "density": 1.5 },
  "grayscale": 16, "color": false,
  "protocol": 1 }

// 渲染完成 + 每次翻页（兼作回执与页码同步）
{ "type": "state", "id": "<内容id>", "page": 2, "pages": 12 }
```

**PC → 设备：**

```jsonc
// 投送排版内容（M1 唯一内容类型）
{ "type": "html", "id": "c-1690000000-ab12", "title": "可为空串", "body": "<p>受控HTML…</p>" }

// 遥控翻页（0-based）
{ "type": "nav", "page": 3 }
```

预留（M1 不实现但解析时未知 type 须静默忽略，保证向前兼容）：`content.begin`/`page`(二进制)/`clear`/`error`。

---

## 9. 已知坑清单（实现方必读）

1. **后台启动 Activity 限制**：内容到达时绝不能从 Service 启动新 Activity（Android 10+ 静默失败）→ 已用单 Activity 方案规避（§5.1）。
2. **CSS px ≠ 物理 px**：WebView `initial-scale=1` 时 1 CSS px = 1 dp，物理字号必须除以 density（§5.4），否则高 DPI 墨水屏上字大一倍。
3. **Ktor 静态文件读不了 assets**：必须自写 asset handler（§4.1）。
4. **evaluateJavascript 转义**：payload 必须经 `JSONObject.quote` 包裹，否则引号/换行注入 JS 报错。
5. **WebView 就绪竞态**：onPageFinished 前收到内容要挂起等待（§5.4）。
6. **JS 桥线程**：`@JavascriptInterface` 回调不在主线程，只做 `tryEmit`，不碰 View。
7. **targetSdk 34 前台服务**必须声明 specialUse 类型与对应权限（§4.3），否则启动即崩。
8. **明文 HTTP**：不加 `usesCleartextTraffic="true"` 的话，发送页里将来 fetch 会被拦（M1 影响小但先加上）。
9. **Ktor META-INF 冲突**：packaging excludes 不加会打包报 duplicate resources。
10. **翻页禁用平滑滚动/过渡**：任何 `transition: transform` 都会在墨水屏上留下一串残影。
11. IP 可能有多个网卡（如开了热点/虚拟网卡）：M1 取第一个 site-local IPv4 即可，但待机页把 URL 明文显示出来供用户核对。
12. 页数计算须在内容插入后的 `requestAnimationFrame`（甚至双 rAF）里做，否则 scrollWidth 为 0。

---

## 10. 验收测试步骤（按序执行）

1. Android Studio 打开工程 → 真机（任意安卓平板/手机可先代替墨水屏）运行。
2. 确认待机页二维码/URL/状态显示正常；通知栏出现常驻通知。
3. 同一 WiFi 下电脑浏览器打开 URL → 顶栏变"已连接"，设备待机页状态变"已连接 1 台电脑"。
4. 依次测试：拖拽网页选中文字 / Ctrl+V / textarea 输入 / 拖入 .txt 文件 → 设备均即时显示，段落缩进、标题、页边距正常。
5. 投送一篇 3000+ 字长文 → 设备分页正确；左右点按翻页无动画；中间点按页脚显隐。
6. 电脑端页码同步、遥控翻页双向一致。
7. 设备返回键回待机；再投一条内容能再次进入阅读模式。
8. 关掉电脑浏览器标签重新打开 → 自动重连；设备连接数正确增减。
9. 拖入一张图片和一个网址 → 电脑端 toast 提示、设备端无反应（预期）。
